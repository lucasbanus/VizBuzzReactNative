import * as React from "react";
import {
  StyleSheet,
  Text,
  View,
  Switch,
  TouchableHighlight
} from "react-native";

const SettingsTab = (props: any) => {
  return <Text>Uploads Page</Text>;
};
